services:
  - type: web
    name: webpage-comments-server
    env: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: MONGODB_URI
        sync: false # set in Render dashboard, do not commit secrets
      - key: PORT
        value: 10000 