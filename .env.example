# MongoDB Connection String
# Get this from MongoDB Atlas:
# 1. Go to https://cloud.mongodb.com/
# 2. Click 'Connect' on your cluster
# 3. Choose 'Connect your application'
# 4. Copy the connection string and replace username and password
MONGODB_URI=mongodb+srv://username:password@cluster0.xxxxx.mongodb.net/database?retryWrites=true&w=majority

# Server Port
# Default: 3001 for local development
# Render.com will use PORT from environment
PORT=3001

# Optional: Cloud Server URL (set this after deploying to Render.com)
# Leave empty for local-only development
# Example: CLOUD_SERVER_URL=https://your-app.onrender.com
CLOUD_SERVER_URL=
